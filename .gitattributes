# Normalize line endings for everyone regardless of OS/core.autocrlf setting.
* text=auto eol=lf

# Windows requires CRLF for its native wrapper/batch launchers.
*.cmd text eol=crlf
*.bat text eol=crlf

# Binary assets: never touch line endings or diff as text.
*.png binary
*.jpg binary
*.jar binary
*.pdf binary
